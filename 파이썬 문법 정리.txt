파이썬

전연변수를 함수 안에서 쓰고 싶으면 전역변수로 표시 해줘야함 ex) global a,b

replace() 함수: 일치하는 문자열을 사용자가 정의한 문자열로 바꾼다. (모든 문자)

299p stat 꼭알고 있기!!

공백으로 입력 받지 않는 것은 map(int,input()) 사용

list.sort() 정렬된 리스트 값을 반환!!

리스트,집합, 딕셔너리 를 함수에 매개변수로 대입하면 call by reference로 부른다!! 

파이썬의 모든 것이 객체이기 때문에 불변객체(int,str)같은 것을 넘겨주면 새로운 값으로 생성한다(밖에 있는것에 영향x)
반면 가변객체(리스트,집합,딕셔너리) 같은경우 새로만들지 않고 주소를 유지해서 받는다(밖에 있는것에 영향o)

map 객체는 두개 이상의 인자를 입력할때 사용하자

관계를 생각할때는 양쪽다 생각해야한다.(1 2 관계가 있다고 생각하면 2 1도 가능하다고 생각해야된다.)

list()는 원소하나만 있으면 안된다. 여러 원소를 가져야 한다.

재귀함수를 쓸때 최대 재귀 늘리는 방법 sys.setrecursionlimit(106)

2차원 리스트 만드는 방법 행 2 열4  [ [0]  열 for _ in range(행) ]

가장 큰 원소 찾기 max

pypy는 재귀에 약하다.

python의 경우에 bisect, bisect_left/right 사용해보시면 이진 탐색 구현없이 사용가능합니다